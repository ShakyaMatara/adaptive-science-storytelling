"""Placeholder — the real implementation replaces this file wholesale.

Until then it delegates to the original progress view so that re-pointing the
URL here changes nothing, and the existing progress test keeps passing.
"""

from . import views

me_progress = views.me_progress
