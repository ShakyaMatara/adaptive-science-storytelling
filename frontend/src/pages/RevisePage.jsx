// Placeholder — replaced with the real page in the next phase.

import { Card, EmptyState } from "../components/ui";

export default function RevisePage() {
  return (
    <Card>
      <EmptyState icon="🎯" title="Revision" message="Targeted practice on the concepts you find hardest. Coming in the next phase." />
    </Card>
  );
}
