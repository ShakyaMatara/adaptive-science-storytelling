// Placeholder — replaced with the real page in the next phase.

import { Card, EmptyState } from "../components/ui";

export default function LibraryPage() {
  return (
    <Card>
      <EmptyState icon="📖" title="My stories" message="Every story you have started, with your points and badges. Coming in the next phase." />
    </Card>
  );
}
