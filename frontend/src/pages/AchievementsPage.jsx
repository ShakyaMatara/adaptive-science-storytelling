// Placeholder — replaced with the real page in the next phase.

import { Card, EmptyState } from "../components/ui";

export default function AchievementsPage() {
  return (
    <Card>
      <EmptyState icon="🏅" title="Achievements" message="Your badge gallery, streaks and totals. Coming in the next phase." />
    </Card>
  );
}
